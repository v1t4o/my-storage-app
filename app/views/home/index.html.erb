<h1>My Storage App</h1>
<h2>Boas vindas ao sistema de gestão de estoques</h2>

<h3>Galpões cadastrados</h3>
<table>
  <tr>
    <th>Nome</th>
    <th>Código</th>
  <tr>
  <% @warehouses.each do |warehouse| %>
    <tr>
      <td><%= link_to(warehouse.name, warehouse_path(warehouse.id)) %></td>
      <td><%= warehouse.code %></td>
    <tr>
  <% end %>
</table>